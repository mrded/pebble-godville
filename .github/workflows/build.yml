name: Pebble Build

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Pebble Docker image
        working-directory: src
        run: docker build --platform linux/amd64 -t pebble-builder .

      - name: Run Pebble build
        working-directory: src
        run: |
          docker run --rm --platform linux/amd64 \
            -v "$(pwd)/src:/app/src:ro" \
            -v "$(pwd)/resources:/app/resources:ro" \
            -v "$(pwd)/appinfo.json:/app/appinfo.json:ro" \
            -v "$(pwd)/wscript:/app/wscript:ro" \
            -v "$(pwd)/build:/app/build" \
            pebble-builder

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: pebble-app
          path: src/build/*.pbw

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: src/build/*.pbw
          generate_release_notes: true
